spring.application.name=demo00
server.port=8081

# Configuracion de la BD MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/escuela?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=Europe/Madrid
spring.datasource.username=root
spring.datasource.password=Saul6220

# JPA / Hibernate: update crea las tablas automaticamente
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Thymeleaf: cache desactivada en desarrollo
spring.thymeleaf.cache=false